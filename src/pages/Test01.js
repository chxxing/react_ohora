import React from 'react';
import { useParams } from 'react-router-dom';
const Test01 = ({ on }) => {
    // const { id } = useParams();
    // const mc = on.find(it=> it.cate == id)
    return (
        <div>
            TEST01
        </div>
    )
}

export default Test01
